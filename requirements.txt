# Server
fastapi==0.111.0
uvicorn==0.29.0

# Repo ingestion
gitpython==3.1.43

# AST parsing — PINNED. 0.22+ has breaking API changes.
tree-sitter==0.21.3
tree-sitter-python==0.21.0
tree-sitter-javascript==0.21.3
tree-sitter-typescript==0.21.2

# Vector DB + embeddings (fastembed extra installs the local embedding model)
qdrant-client[fastembed]==1.9.1

# LLM — Groq via OpenAI-compatible SDK
openai==1.30.1

# Graph visualisation
networkx==3.3
pyvis==0.3.2

# Utilities
python-dotenv==1.0.1
httpx==0.27.0